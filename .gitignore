node_modules
dist
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.sqlite-journal
*.db
.DS_Store
.env
tmp

# downloadable corpus source (re-fetch via npm run corpus:download)
apps/server/data/

# source books (copyrighted PDFs/scans — keep out of git)
sources/

# static-build artifacts (generated by prepare:static). The published
# database IS committed (negated below) — Pages CI can't rebuild the Wilson
# layer because its source text stays out of git. Refresh it with
# `npm run db:publish`.
apps/web/public/sqlite.worker.js
apps/web/public/sql-wasm.wasm
!apps/web/public/typologos-public.sqlite
